// to move the snake in the desired direction just add the x and y of the direction 

const RIGHT = [0, 1]
const LEFT = [0, -1]
const UP = [-1, 0]
const DOWN = [1, 0]

export default {RIGHT,LEFT,UP,DOWN}